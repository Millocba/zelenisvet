# Rediseño zelenisvet.com.ar Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rebuild the one-page site of ZELENI SVET Consultora with identical Spanish content, a "Verde Energía" visual identity, hardened security (CSP headers, hardened `enviar.php`, zero third-party deps) and improved UX (responsive, a11y, WhatsApp, map, mailto).

**Architecture:** Pure static HTML/CSS/JS (no build tools, no frameworks, no CDN deps) plus a single hardened PHP endpoint for the contact form. Security enforced via `.htaccess` headers + CSP; the PHP form uses CSRF tokens, rate limiting, honeypot and strict validation.

**Tech Stack:** HTML5, CSS3 (custom properties), vanilla JS (ES6, IIFE), PHP 8 (mail-friendly), Apache `.htaccess`. Served on Hostinger shared hosting.

**Spec:** `docs/superpowers/specs/2026-09-12-zelenisvet-redesign-design.md`

**Working dir:** `D:\Diagrama`. Assets already downloaded to `assets/img/` (logo1.png, logoCertificado.png, imagen1-4.jpeg, banner2.jpeg).

---

## File Structure

| File | Responsibility |
|---|---|
| `index.html` | One-page markup with original content, semantic HTML5, no inline JS/CSS |
| `css/fonts.css` | `@font-face` for self-hosted latin subsets (generated by script) |
| `css/styles.css` | Full design system + components + responsive + a11y |
| `js/main.js` | Menu, scroll effects, reveal animations, form fetch with CSRF token |
| `enviar.php` | Session CSRF, token endpoint, rate limit, honeypot, validation, mail() |
| `.htaccess` | HTTPS, security headers + CSP, deny dotfiles, no index listing |
| `404.html`, `robots.txt` | Minimal error page and crawl rules |
| `assets/fonts/*.woff2` | Self-hosted Montserrat (600/700) + Open Sans (400/600) latin |
| `assets/img/favicon.svg` | Sprout favicon |
| `scripts/fetch-fonts.ps1` | Helper to download latin `woff2` and generate `css/fonts.css` |

---

## Task 1: Self-host fonts (latin subsets)

**Files:**
- Create: `scripts/fetch-fonts.ps1`
- Create: `css/fonts.css`
- Create: `assets/fonts/` (output)

- [ ] **Step 1: Write the PowerShell helper**

Create `scripts/fetch-fonts.ps1`:

```powershell
$ErrorActionPreference = 'Stop'
$root = Split-Path -Parent $PSScriptRoot
$fontDir = Join-Path $root 'assets\fonts'
$cssFile = Join-Path $root 'css\fonts.css'
New-Item -ItemType Directory -Force -Path $fontDir | Out-Null

$families = @(
  'family=Montserrat:wght@600;700',
  'family=Open+Sans:wght@400;600'
)
$ua = 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/125.0.0.0 Safari/537.36'
$url = 'https://fonts.googleapis.com/css2?' + ($families -join '&') + '&display=swap'

$css = (Invoke-WebRequest -Uri $url -Headers @{ 'User-Agent' = $ua } -UseBasicParsing).Content

$blocks = [regex]::Matches($css, '@font-face\s*{(?s)(.*?)}')
$out = @()
$out += '/* Self-hosted latin subsets - generated by scripts/fetch-fonts.ps1 */'
$out += ''

foreach ($m in $blocks) {
  $body = $m.Groups[1].Value
  $family = [regex]::Match($body, 'font-family:\s*(?:\x27|\x22)?([^\x27\x22;]+?)(?:\x27|\x22)?;').Groups[1].Value.Trim()
  $weight = [regex]::Match($body, 'font-weight:\s*(\d+)').Groups[1].Value
  $style = [regex]::Match($body, 'font-style:\s*(\w+)').Groups[1].Value
  $range = [regex]::Match($body, 'unicode-range:\s*([^;]+);').Groups[1].Value.Trim()
  $src = [regex]::Match($body, 'url\((https://[^)]+\.woff2)\)').Groups[1].Value

  # Only latin subset block
  if ($range -notmatch 'U\+0000-00FF') { continue }

  $file = ($family -replace '\s+', '-') + '-' + $weight + '.woff2'
  Invoke-WebRequest -Uri $src -OutFile (Join-Path $fontDir $file) -UseBasicParsing

  $out += "@font-face {"
  $out += "  font-family: '$family';"
  $out += "  font-style: $style;"
  $out += "  font-weight: $weight;"
  $out += "  font-display: swap;"
  $out += "  src: url('../assets/fonts/$file') format('woff2');"
  $out += "  unicode-range: $range;"
  $out += "}"
  $out += ""
}

Set-Content -Path $cssFile -Value $out -Encoding UTF8
Write-Output "fonts written to $cssFile"
Get-ChildItem $fontDir | Select-Object Name, Length
```

- [ ] **Step 2: Run the script**

Run: `powershell -ExecutionPolicy Bypass -File scripts/fetch-fonts.ps1`
Expected: 4 files in `assets/fonts/` — `Montserrat-600.woff2`, `Montserrat-700.woff2`, `Open Sans-400.woff2`, `Open Sans-600.woff2` — each between ~10–25 KB, and `css/fonts.css` created with 4 `@font-face` blocks.

- [ ] **Step 3: Commit**

```bash
git add scripts/fetch-fonts.ps1 css/fonts.css assets/fonts
git commit -m "feat: self-host Montserrat + Open Sans latin subsets"
```

---

## Task 2: Favicon

**Files:**
- Create: `assets/img/favicon.svg`

- [ ] **Step 1: Write the SVG favicon**

Create `assets/img/favicon.svg`:

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <rect width="64" height="64" rx="14" fill="#1B5E20"/>
  <path d="M32 54C20 44 13 32 14 21c11-4 21 1 24 11 3 11-1 17-6 22z" fill="#4CAF50"/>
  <path d="M34 52c8-10 14-23 10-35 9 7 11 21 2 35z" fill="#66BB6A"/>
</svg>
```

- [ ] **Step 2: Commit**

```bash
git add assets/img/favicon.svg
git commit -m "feat: add favicon"
```

---

## Task 3: `css/styles.css` (design system + components)

**Files:**
- Create: `css/styles.css`

- [ ] **Step 1: Write the stylesheet**

Create `css/styles.css` (complete):

```css
:root {
  --green-900: #1b5e20;
  --green-700: #2e7d32;
  --green-500: #4caf50;
  --green-300: #66bb6a;
  --green-100: #e8f5e9;
  --green-border: #c8e6c9;
  --ink: #212121;
  --ink-soft: #424242;
  --white: #ffffff;
  --shadow: 0 4px 14px rgba(27, 94, 32, .12);
  --shadow-lg: 0 10px 30px rgba(27, 94, 32, .18);
  --radius: 14px;
  --container: 1140px;
  --header-h: 72px;
  --font-h: 'Montserrat', system-ui, sans-serif;
  --font-b: 'Open Sans', system-ui, sans-serif;
}

*, *::before, *::after { box-sizing: border-box; }

html { scroll-behavior: smooth; }

body {
  margin: 0;
  font-family: var(--font-b);
  color: var(--ink);
  background: var(--white);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}

img { max-width: 100%; height: auto; display: block; }

h1, h2, h3 { font-family: var(--font-h); color: var(--green-900); line-height: 1.2; margin: 0 0 1rem; }

h1 { font-size: clamp(2rem, 5vw, 3.2rem); font-weight: 700; }
h2 { font-size: clamp(1.6rem, 3.5vw, 2.3rem); font-weight: 700; }
h3 { font-size: 1.15rem; font-weight: 600; }

p { margin: 0 0 1rem; }

a { color: var(--green-700); }

:focus-visible {
  outline: 3px solid var(--green-500);
  outline-offset: 2px;
  border-radius: 4px;
}

.container { width: min(var(--container), 92%); margin-inline: auto; }

.section { padding: 4.5rem 0; }
.section--tint { background: var(--green-100); }
.section-title { text-align: center; margin-bottom: .5rem; position: relative; }
.section-sub { text-align: center; color: var(--ink-soft); max-width: 640px; margin: 0 auto 2.6rem; }

/* ---------- Header ---------- */
.site-header {
  position: sticky;
  top: 0;
  z-index: 100;
  background: var(--green-900);
  box-shadow: none;
  transition: box-shadow .25s ease;
}
.site-header.scrolled { box-shadow: var(--shadow-lg); }

.navbar { display: flex; align-items: center; justify-content: space-between; min-height: var(--header-h); }

.brand { display: flex; align-items: center; text-decoration: none; }
.brand img { height: 46px; width: auto; border-radius: 8px; background: var(--white); padding: 4px; }

.nav { display: flex; gap: 1.4rem; margin: 0; padding: 0; list-style: none; }
.nav a {
  color: rgba(255, 255, 255, .9);
  text-decoration: none;
  font-weight: 600;
  padding: .4rem .2rem;
  border-bottom: 2px solid transparent;
}
.nav a:hover, .nav a:focus-visible { color: #fff; border-color: var(--green-500); }

.menu-toggle {
  display: none;
  background: none; border: 0; cursor: pointer;
  width: 44px; height: 44px; padding: 10px;
}
.menu-toggle span { display: block; height: 3px; width: 100%; background: #fff; border-radius: 3px; transition: transform .25s ease, opacity .25s ease; }
.menu-toggle span + span { margin-top: 6px; }
.menu-toggle.open span:nth-child(1) { transform: translateY(9px) rotate(45deg); }
.menu-toggle.open span:nth-child(2) { opacity: 0; }
.menu-toggle.open span:nth-child(3) { transform: translateY(-9px) rotate(-45deg); }

/* ---------- Hero ---------- */
.hero {
  position: relative;
  min-height: calc(100vh - var(--header-h));
  display: flex;
  align-items: center;
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  color: #fff;
}
.hero::before {
  content: '';
  position: absolute; inset: 0;
  background: linear-gradient(115deg, rgba(27, 94, 32, .88) 0%, rgba(27, 94, 32, .55) 55%, rgba(27, 94, 32, .25) 100%);
}
.hero .container { position: relative; z-index: 1; }
.hero h1 { color: #fff; }
.hero p { font-size: 1.2rem; max-width: 560px; color: rgba(255, 255, 255, .92); }
.hero .hero-tag { letter-spacing: .14em; text-transform: uppercase; font-size: .8rem; color: var(--green-300); font-weight: 700; margin-bottom: .75rem; }
.section--tint .cta-text, .cta-text { text-align: center; margin-top: 2.4rem; }
.cta-text strong { color: var(--green-900); }

/* ---------- Buttons ---------- */
.btn {
  display: inline-block;
  background: var(--green-500);
  color: #fff;
  font-family: var(--font-b);
  font-weight: 700;
  text-decoration: none;
  padding: .85rem 1.7rem;
  border-radius: 40px;
  border: 0;
  cursor: pointer;
  transition: background .2s ease, transform .2s ease;
}
.btn:hover { background: var(--green-300); transform: translateY(-2px); color: #fff; }

/* ---------- Services grid ---------- */
.services-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 1.4rem;
}
.service-card {
  background: var(--white);
  border: 1px solid var(--green-border);
  border-radius: var(--radius);
  padding: 1.6rem;
  box-shadow: var(--shadow);
  transition: transform .25s ease, box-shadow .25s ease;
  display: flex;
  flex-direction: column;
}
.service-card:hover { transform: translateY(-6px); box-shadow: var(--shadow-lg); }
.service-card .icon {
  width: 48px; height: 48px;
  background: var(--green-100);
  border-radius: 12px;
  display: flex; align-items: center; justify-content: center;
  margin-bottom: 1rem;
}
.service-card .icon svg { width: 26px; height: 26px; stroke: var(--green-700); fill: none; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }
.service-card p { color: var(--ink-soft); margin: 0; font-size: .95rem; }

/* ---------- Projects ---------- */
.project-gallery {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
  gap: 1.4rem;
  margin-top: 2rem;
}
.project-item {
  position: relative;
  border-radius: var(--radius);
  overflow: hidden;
  box-shadow: var(--shadow);
  background: var(--green-900);
}
.project-item img { width: 100%; aspect-ratio: 4 / 3; object-fit: cover; transition: transform .4s ease; }
.project-item:hover img { transform: scale(1.05); }
.project-item figcaption {
  position: absolute;
  inset: auto 0 0 0;
  padding: 2.2rem 1rem .9rem;
  color: #fff;
  font-weight: 600;
  background: linear-gradient(transparent, rgba(27, 94, 32, .9));
}
.banner { position: relative; overflow: hidden; }
.banner img { width: 100%; max-height: 320px; object-fit: cover; }
.sub-project-title { text-align: center; margin-top: 1.6rem; }

/* ---------- Contact ---------- */
.contact-wrapper {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 2.4rem;
  align-items: start;
}
.contact-info .contact-line { display: flex; align-items: center; gap: .6rem; margin-bottom: .9rem; color: var(--ink-soft); }
.contact-info .contact-line svg { width: 22px; height: 22px; stroke: var(--green-700); fill: none; stroke-width: 2; }
.contact-info a { color: var(--green-700); font-weight: 600; text-decoration: none; }
.contact-info a:hover { text-decoration: underline; }
.cert-logo { text-align: center; margin-top: 1.4rem; }
.cert-logo img { max-width: 150px; margin: 0 auto .5rem; }
.cert-logo p { font-size: .85rem; color: var(--ink-soft); font-weight: 700; }

.contact-form {
  background: var(--white);
  border: 1px solid var(--green-border);
  border-radius: var(--radius);
  padding: 2rem;
  box-shadow: var(--shadow);
}
.contact-form label { display: block; font-weight: 600; color: var(--green-900); margin: 1rem 0 .35rem; }
.contact-form input[type='text'],
.contact-form input[type='email'],
.contact-form input[type='tel'],
.contact-form textarea {
  width: 100%;
  padding: .75rem .9rem;
  border: 1.5px solid var(--green-border);
  border-radius: 10px;
  font-family: var(--font-b);
  font-size: 1rem;
  color: var(--ink);
  background: var(--white);
  transition: border-color .2s ease, box-shadow .2s ease;
}
.contact-form textarea { resize: vertical; min-height: 130px; }
.contact-form input:focus, .contact-form textarea:focus {
  outline: none;
  border-color: var(--green-500);
  box-shadow: 0 0 0 4px rgba(76, 175, 80, .18);
}
.contact-form .btn { margin-top: 1.4rem; width: 100%; }
.contact-form .btn:disabled { opacity: .55; cursor: not-allowed; transform: none; }
.hp-field { position: absolute !important; left: -9999px !important; top: auto; height: 1px; width: 1px; overflow: hidden; }
.form-response { margin-top: 1rem; padding: .8rem 1rem; border-radius: 10px; font-weight: 600; display: none; }
.form-response--ok { background: var(--green-100); color: var(--green-900); border: 1px solid var(--green-border); display: block; }
.form-response--error { background: #fdecea; color: #b71c1c; border: 1px solid #ffcdd2; display: block; }

.map-frame { margin-top: 2rem; border-radius: var(--radius); overflow: hidden; box-shadow: var(--shadow); }
.map-frame iframe { width: 100%; height: 320px; border: 0; display: block; }

/* ---------- Footer ---------- */
.site-footer { background: var(--green-900); color: rgba(255, 255, 255, .85); text-align: center; padding: 2.2rem 0; }
.site-footer p { margin: .3rem 0; }

/* ---------- Floating buttons ---------- */
.fab {
  position: fixed;
  right: 20px;
  z-index: 90;
  width: 56px; height: 56px;
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  box-shadow: var(--shadow-lg);
  transition: transform .2s ease;
}
.fab:hover { transform: translateY(-3px); }
.fab-whatsapp { bottom: 24px; background: #25d366; color: #fff; }
.fab-whatsapp svg { width: 30px; height: 30px; fill: #fff; }
.fab-top {
  bottom: 92px;
  background: var(--green-500);
  border: 0; cursor: pointer;
  opacity: 0;
  pointer-events: none;
  transition: opacity .3s ease, transform .2s ease;
}
.fab-top.show { opacity: 1; pointer-events: auto; }
.fab-top svg { width: 24px; height: 24px; stroke: #fff; fill: none; stroke-width: 2.5; }

/* ---------- Reveal animation ---------- */
.reveal { opacity: 0; transform: translateY(20px); transition: opacity .6s ease, transform .6s ease; }
.reveal.visible { opacity: 1; transform: none; }

/* ---------- Responsive ---------- */
@media (max-width: 900px) {
  .contact-wrapper { grid-template-columns: 1fr; }
  .section { padding: 3.2rem 0; }
}

@media (max-width: 768px) {
  .menu-toggle { display: block; margin-left: auto; }
  .nav {
    position: fixed;
    inset: var(--header-h) 0 auto 0;
    flex-direction: column;
    gap: 0;
    background: var(--green-900);
    padding: .6rem 0 1.2rem;
    transform: translateY(-120%);
    transition: transform .3s ease;
    box-shadow: var(--shadow-lg);
  }
  .nav.active { transform: translateY(0); }
  .nav a { display: block; padding: .85rem min(5%, 2rem); border-bottom: 1px solid rgba(255, 255, 255, .12); }
  .hero { min-height: 70vh; }
  .btn { width: 100%; text-align: center; }
}

@media (prefers-reduced-motion: reduce) {
  html { scroll-behavior: auto; }
  .reveal { opacity: 1; transform: none; transition: none; }
  .service-card, .project-item, .fab { transition: none; }
}
```

- [ ] **Step 2: Commit**

```bash
git add css/styles.css
git commit -m "feat: complete stylesheet with design system and components"
```

---

## Task 4: `index.html` (full page, original content)

**Files:**
- Create: `index.html`

- [ ] **Step 1: Write the page**

Create `index.html` with the exact original Spanish content. Complete content:

```html
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ZELENI SVET Consultora - Soluciones Sostenibles</title>
  <meta name="description" content="Consultora especializada en soluciones sostenibles y ecológicas para empresas y particulares. Proyectos de impacto ambiental, auditoría y gestión ambiental.">
  <link rel="icon" href="assets/img/favicon.svg" type="image/svg+xml">
  <link rel="preload" href="assets/fonts/Montserrat-700.woff2" as="font" type="font/woff2" crossorigin>
  <link rel="stylesheet" href="css/fonts.css">
  <link rel="stylesheet" href="css/styles.css">
  <script src="js/main.js" defer></script>
</head>
<body>

  <header class="site-header" id="site-header">
    <div class="container navbar">
      <a class="brand" href="#inicio" aria-label="ZELENI SVET - inicio">
        <img src="assets/img/logo1.png" alt="Logo ZELENI SVET" width="46" height="46">
      </a>
      <button class="menu-toggle" id="menu-toggle" aria-label="Abrir menú" aria-expanded="false" aria-controls="main-nav">
        <span></span><span></span><span></span>
      </button>
      <nav id="main-nav" aria-label="Navegación principal">
        <ul class="nav">
          <li><a href="#inicio">Inicio</a></li>
          <li><a href="#servicios">Servicios</a></li>
          <li><a href="#proyectos">Proyectos</a></li>
          <li><a href="#contacto">Contacto</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <main>
    <section id="inicio" class="hero">
      <div class="container">
        <p class="hero-tag">Consultora en soluciones sostenibles</p>
        <h1>Bienvenidos a ZELENI SVET Consultora</h1>
        <p>Somos una consultora especializada en soluciones sostenibles y ecológicas para empresas y particulares. ¡Tu futuro es verde!</p>
        <a href="#contacto" class="btn">Más Información</a>
      </div>
    </section>

    <section id="servicios" class="section section--tint">
      <div class="container">
        <h2 class="section-title">Nuestros Servicios Clave</h2>

        <div class="services-grid">
          <article class="service-card reveal">
            <div class="icon"><svg viewBox="0 0 24 24"><path d="M11 20A7 7 0 0 1 9.8 6.1C15.5 5 17 4.5 19 2c1 2 2 4.2 2 8 0 5.5-4.8 10-10 10Z"/><path d="M2 21c0-3 1.8-5.5 4.5-6.5"/></svg></div>
            <h3>Asesoramiento Sostenible</h3>
            <p>Guía experta para implementar <strong>prácticas empresariales ecológicas</strong> y responsables, asegurando el cumplimiento normativo.</p>
          </article>

          <article class="service-card reveal">
            <div class="icon"><svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/><line x1="4.2" y1="4.2" x2="5.6" y2="5.6"/><line x1="18.4" y1="18.4" x2="19.8" y2="19.8"/><line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/><line x1="4.2" y1="19.8" x2="5.6" y2="18.4"/><line x1="18.4" y1="5.6" x2="19.8" y2="4.2"/></svg></div>
            <h3>Tecnologías Verdes</h3>
            <p>Implementación de soluciones innovadoras y eficientes para reducir el impacto ambiental.</p>
          </article>

          <article class="service-card reveal">
            <div class="icon"><svg viewBox="0 0 24 24"><path d="M22 10 12 5 2 10l10 5 10-5Z"/><path d="M6 12v5c3 3 9 3 12 0v-5"/></svg></div>
            <h3>Formación y Talleres</h3>
            <p>Capacitación a tu equipo en temas clave de sostenibilidad y gestión ambiental.</p>
          </article>

          <article class="service-card reveal">
            <div class="icon"><svg viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8Z"/><path d="M14 2v6h6"/><line x1="9" y1="15" x2="15" y2="15"/><line x1="9" y1="11" x2="11" y2="11"/></svg></div>
            <h3>Aviso de Proyectos y Estudio de Impacto Ambiental (EsIA)</h3>
            <p>Gestión de la documentación y realización de estudios necesarios para la aprobación legal de tus proyectos.</p>
          </article>

          <article class="service-card reveal">
            <div class="icon"><svg viewBox="0 0 24 24"><path d="M9 5H7a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V7a2 2 0 0 0-2-2h-2"/><rect x="9" y="3" width="6" height="4" rx="1"/><line x1="9" y1="12" x2="15" y2="12"/><line x1="9" y1="16" x2="13" y2="16"/></svg></div>
            <h3>Auditoría Ambiental</h3>
            <p>Evaluación detallada del cumplimiento normativo y del desempeño ambiental de tus operaciones y procesos.</p>
          </article>

          <article class="service-card reveal">
            <div class="icon"><svg viewBox="0 0 24 24"><path d="M12 15a3 3 0 1 0 0-6 3 3 0 0 0 0 6Z"/><path d="M19.4 15a1.7 1.7 0 0 0 .3 1.9L20 17.3a2 2 0 0 1-2.8 2.8l-.4-.3a1.7 1.7 0 0 0-1.9-.3 1.7 1.7 0 0 0-1 1.5V21a2 2 0 0 1-4 0v-.1a1.7 1.7 0 0 0-1-1.6 1.7 1.7 0 0 0-1.9.4l-.4.3a2 2 0 0 1-2.8-2.8l.3-.4a1.7 1.7 0 0 0 .3-1.9 1.7 1.7 0 0 0-1.5-1H3a2 2 0 0 1 0-4h.1a1.7 1.7 0 0 0 1.6-1 1.7 1.7 0 0 0-.4-1.9l-.3-.4a2 2 0 0 1 2.8-2.8l.4.3a1.7 1.7 0 0 0 1.9.3h.1a1.7 1.7 0 0 0 1-1.5V3a2 2 0 0 1 4 0v.1a1.7 1.7 0 0 0 1 1.5h.1a1.7 1.7 0 0 0 1.9-.3l.4-.3a2 2 0 0 1 2.8 2.8l-.3.4a1.7 1.7 0 0 0-.3 1.9v.1a1.7 1.7 0 0 0 1.5 1H21a2 2 0 0 1 0 4h-.1a1.7 1.7 0 0 0-1.5 1Z"/></svg></div>
            <h3>Plan de Gestión Ambiental (PGA)</h3>
            <p>Diseño e implementación de estrategias para mitigar impactos, optimizar recursos y mejorar continuamente tu gestión ecológica.</p>
          </article>

          <article class="service-card reveal">
            <div class="icon"><svg viewBox="0 0 24 24"><path d="M22 12h-4l-3 9L9 3l-3 9H2"/></svg></div>
            <h3>Monitoreo</h3>
            <p>Medición y seguimiento constante de indicadores ambientales clave (agua, aire, residuos) para asegurar el control y cumplimiento.</p>
          </article>

          <article class="service-card reveal">
            <div class="icon"><svg viewBox="0 0 24 24"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg></div>
            <h3>Soluciones Basadas en la Naturaleza (SbN)</h3>
            <p>Implementación de acciones inspiradas y apoyadas por la naturaleza, como techos verdes o jardines verticales, para abordar desafíos sociales y ambientales.</p>
          </article>

          <article class="service-card reveal">
            <div class="icon"><svg viewBox="0 0 24 24"><path d="m9 18 6-6-6-6"/></svg></div>
            <h3>Dispositivos de Educación Ambiental</h3>
            <p>Creación e instalación de herramientas y materiales interactivos para promover la conciencia y la acción sostenible en espacios públicos o privados.</p>
          </article>

          <article class="service-card reveal">
            <div class="icon"><svg viewBox="0 0 24 24"><line x1="18" y1="20" x2="18" y2="10"/><line x1="12" y1="20" x2="12" y2="4"/><line x1="6" y1="20" x2="6" y2="14"/></svg></div>
            <h3>Observatorio Urbano / Diagnóstico</h3>
            <p>Análisis y recopilación de datos para generar un diagnóstico completo de la sostenibilidad urbana o territorial, guiando la planificación futura.</p>
          </article>
        </div>

        <p class="cta-text"><strong>¿Necesitas una solución específica?</strong> Explora cómo nuestros servicios pueden impulsar la sostenibilidad de tu proyecto.</p>
      </div>
    </section>

    <section id="proyectos" class="section">
      <div class="container">
        <h2 class="section-title">Casos de Éxito y Proyectos</h2>
        <p class="section-sub">Ayudamos a las empresas a alcanzar sus objetivos de sostenibilidad. Desde la optimización de recursos hídricos hasta la certificación LEED de edificios.</p>
        <h2 class="sub-project-title">Proyecto premiado por la Universidad Católica de Córdoba</h2>

        <div class="project-gallery">
          <figure class="project-item">
            <img src="assets/img/imagen1.jpeg" alt="Edificio moderno" loading="lazy">
            <figcaption>Certificación LEED Platino</figcaption>
          </figure>
          <figure class="project-item">
            <img src="assets/img/imagen2.jpeg" alt="Sistema de agua" loading="lazy">
            <figcaption>Gestión y ahorro de agua</figcaption>
          </figure>
          <figure class="project-item">
            <img src="assets/img/imagen3.jpeg" alt="Paneles solares" loading="lazy">
            <figcaption>Instalación de energía solar</figcaption>
          </figure>
          <figure class="project-item">
            <img src="assets/img/imagen4.jpeg" alt="Jardín vertical" loading="lazy">
            <figcaption>Diseño biofílico y bienestar</figcaption>
          </figure>
        </div>

        <p class="cta-text">¡Contáctanos para conocer nuestros últimos trabajos!</p>
      </div>
    </section>

    <div class="banner">
      <img src="assets/img/banner2.jpeg" alt="Banner de proyecto de sostenibilidad" loading="lazy">
    </div>

    <section id="contacto" class="section section--tint">
      <div class="container">
        <h2 class="section-title">Pongámonos en Contacto</h2>

        <div class="contact-wrapper">
          <div class="contact-info">
            <h3>¿Listo para el Cambio? Contáctanos.</h3>
            <p>Para más información, o para solicitar una cotización sin compromiso, no dudes en escribirnos.</p>

            <p class="contact-line">
              <svg viewBox="0 0 24 24"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="m22 7-10 6L2 7"/></svg>
              <a href="mailto:informacion@zelenisvet.com.ar">informacion@zelenisvet.com.ar</a>
            </p>
            <p class="contact-line">
              <svg viewBox="0 0 24 24"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.13.96.36 1.9.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.91.34 1.85.57 2.81.7A2 2 0 0 1 22 16.92Z"/></svg>
              <a href="https://wa.me/5493513001702" rel="noopener" target="_blank">351-3001702 / 351-7533434</a>
            </p>

            <div class="cert-logo">
              <img src="assets/img/logoCertificado.png" alt="Logo certificado RNCEA">
              <p>Certificación RNCEA</p>
            </div>
          </div>

          <div class="contact-form" id="respuesta-container">
            <h3>Envíanos un Mensaje</h3>

            <form id="contactForm" novalidate>
              <div class="hp-field" aria-hidden="true">
                <label for="website">No completar</label>
                <input id="website" type="text" name="website" tabindex="-1" autocomplete="off">
              </div>
              <input type="hidden" name="csrf" id="csrf-value">

              <label for="nombre">Nombre:</label>
              <input type="text" id="nombre" name="nombre" maxlength="60" required autocomplete="name">

              <label for="email">Correo Electrónico:</label>
              <input type="email" id="email" name="email" maxlength="120" required autocomplete="email">

              <label for="telefono">Teléfono:</label>
              <input type="tel" id="telefono" name="telefono" placeholder="Ingresa tu teléfono" required autocomplete="tel">

              <label for="mensaje">Mensaje:</label>
              <textarea id="mensaje" name="mensaje" rows="5" minlength="10" maxlength="2000" required></textarea>

              <button type="submit" class="btn" id="enviar-btn">Enviar Consulta</button>
              <div id="respuesta" class="form-response" role="status"></div>
            </form>
          </div>
        </div>

        <div class="map-frame">
          <iframe
            src="https://www.google.com/maps?q=C%C3%B3rdoba,%20C%C3%B3rdoba,%20Argentina&z=13&output=embed"
            title="Mapa de ubicación en Córdoba"
            loading="lazy"
            referrerpolicy="no-referrer-when-downgrade"
            allowfullscreen></iframe>
        </div>
      </div>
    </section>
  </main>

  <footer class="site-footer">
    <div class="container">
      <p>&copy; 2025 ZELENI SVET Consultora. Todos los derechos reservados.</p>
      <p>Diseño Web Sostenible.</p>
    </div>
  </footer>

  <a class="fab fab-whatsapp" href="https://wa.me/5493513001702" target="_blank" rel="noopener" aria-label="Escribinos por WhatsApp">
    <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M17.47 14.38c-.3-.15-1.76-.87-2.03-.97-.27-.1-.47-.15-.67.15-.2.3-.77.97-.94 1.17-.17.2-.35.22-.65.07-.3-.15-1.26-.46-2.4-1.47-.88-.79-1.48-1.76-1.65-2.06-.17-.3-.02-.46.13-.61.14-.13.3-.35.45-.52.15-.17.2-.3.3-.5.1-.2.05-.37-.02-.52-.08-.15-.67-1.62-.92-2.22-.24-.58-.49-.5-.67-.51h-.57c-.2 0-.52.07-.8.37-.27.3-1.04 1.02-1.04 2.5 0 1.47 1.07 2.89 1.22 3.09.15.2 2.1 3.2 5.1 4.49.71.3 1.27.49 1.7.63.72.23 1.37.2 1.88.12.57-.09 1.76-.72 2.01-1.42.25-.7.25-1.29.17-1.42-.07-.12-.27-.2-.57-.34Z"/><path d="M12.05 2a9.9 9.9 0 0 0-8.4 15.1L2 22l5-1.3A9.9 9.9 0 1 0 12.05 2Zm0 18.1a8.2 8.2 0 0 1-4.18-1.15l-.3-.18-3 0 .8-2.9-.2-.32a8.2 8.2 0 1 1 6.9 4.55Z"/></svg>
  </a>

  <button class="fab fab-top" id="back-to-top" aria-label="Volver arriba">
    <svg viewBox="0 0 24 24" aria-hidden="true"><path d="m18 15-6-6-6 6"/></svg>
  </button>

</body>
</html>
```

> **Note:** The hero background is applied via CSS against `assets/img/banner2.jpeg` (inline `style` would violate the strict CSP `style-src 'self'`).

- [ ] **Step 2: Add the hero background to the stylesheet**

In `css/styles.css`, add a rule so the hero uses `banner2.jpeg`:

```css
.hero { background-image: url('../assets/img/banner2.jpeg'); }
```

- [ ] **Step 3: Commit**

```bash
git add index.html css/styles.css
git commit -m "feat: full one-page markup with original content"
```

---

## Task 5: `js/main.js`

**Files:**
- Create: `js/main.js`

- [ ] **Step 1: Write the script**

Create `js/main.js` (vanilla, IIFE, no dependencies):

```js
(function () {
  'use strict';

  var menuToggle = document.getElementById('menu-toggle');
  var mainNav = document.getElementById('main-nav');

  if (menuToggle && mainNav) {
    menuToggle.addEventListener('click', function () {
      var open = mainNav.classList.toggle('active');
      menuToggle.classList.toggle('open', open);
      menuToggle.setAttribute('aria-expanded', open ? 'true' : 'false');
    });
    mainNav.addEventListener('click', function (e) {
      if (e.target.closest('a')) {
        mainNav.classList.remove('active');
        menuToggle.classList.remove('open');
        menuToggle.setAttribute('aria-expanded', 'false');
      }
    });
  }

  var header = document.getElementById('site-header');
  var onScroll = function () {
    if (header) header.classList.toggle('scrolled', window.scrollY > 10);
  };
  window.addEventListener('scroll', onScroll, { passive: true });
  onScroll();

  var backToTop = document.getElementById('back-to-top');
  if (backToTop) {
    var toggleTop = function () {
      backToTop.classList.toggle('show', window.scrollY > 600);
    };
    window.addEventListener('scroll', toggleTop, { passive: true });
    toggleTop();
    backToTop.addEventListener('click', function () {
      window.scrollTo({ top: 0, behavior: 'smooth' });
    });
  }

  var reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  var revealEls = document.querySelectorAll('.reveal');
  if (revealEls.length) {
    if (reduceMotion || !('IntersectionObserver' in window)) {
      Array.prototype.forEach.call(revealEls, function (el) { el.classList.add('visible'); });
    } else {
      var io = new IntersectionObserver(function (entries) {
        entries.forEach(function (entry) {
          if (entry.isIntersecting) {
            entry.target.classList.add('visible');
            io.unobserve(entry.target);
          }
        });
      }, { threshold: 0.12 });
      Array.prototype.forEach.call(revealEls, function (el) { io.observe(el); });
    }
  }

  var form = document.getElementById('contactForm');
  var responseBox = document.getElementById('respuesta');
  var submitBtn = document.getElementById('enviar-btn');

  if (!form) return;

  function setResponse(msg, isError) {
    if (!responseBox) return;
    responseBox.textContent = msg;
    responseBox.className = 'form-response ' + (isError ? 'form-response--error' : 'form-response--ok');
  }

  function loadToken() {
    return fetch('enviar.php?action=token', { headers: { 'Accept': 'application/json' } })
      .then(function (res) {
        if (!res.ok) throw new Error('token');
        return res.json();
      })
      .then(function (data) {
        form.elements['csrf'].value = data.csrf_token;
        if (submitBtn) submitBtn.disabled = false;
      })
      .catch(function () {
        setResponse('Hubo un error al conectar con el servidor. Escribinos directamente a informacion@zelenisvet.com.ar', true);
      });
  }

  if (submitBtn) submitBtn.disabled = true;
  loadToken();

  form.addEventListener('submit', function (e) {
    e.preventDefault();
    setResponse('', false);

    fetch('enviar.php', {
      method: 'POST',
      body: new FormData(form)
    })
      .then(function (res) {
        return res.json().then(function (data) {
          if (!res.ok) throw { message: data.message || 'Hubo un error al enviar el mensaje.' };
          return data.message;
        });
      })
      .then(function (msg) {
        setResponse(msg, false);
        form.reset();
      })
      .catch(function (err) {
        setResponse(err.message || 'Hubo un error al enviar el mensaje. Escribinos directamente a informacion@zelenisvet.com.ar', true);
      });
  });
})();
```

- [ ] **Step 2: Commit**

```bash
git add js/main.js
git commit -m "feat: vanilla JS interactivity and hardened form handling"
```

---

## Task 6: `enviar.php` (hardened endpoint)

**Files:**
- Create: `enviar.php`

- [ ] **Step 1: Write the PHP**

Create `enviar.php`:

```php
<?php
declare(strict_types=1);

session_start();

header('Content-Type: application/json; charset=utf-8');

const MAX_RATE = 5;
const RATE_WINDOW = 3600;
const DESTINATION = 'informacion@zelenisvet.com.ar';

if (empty($_SESSION['csrf'])) {
    $_SESSION['csrf'] = bin2hex(random_bytes(32));
}
$csrf = $_SESSION['csrf'];

function fail(int $code, string $message): void {
    http_response_code($code);
    echo json_encode(['ok' => false, 'message' => $message], JSON_UNESCAPED_UNICODE);
    exit;
}

function ok(string $message, array $extra = []): void {
    echo json_encode(['ok' => true, 'message' => $message] + $extra, JSON_UNESCAPED_UNICODE);
    exit;
}

$method = $_SERVER['REQUEST_METHOD'] ?? 'GET';

if ($method === 'GET' && (($_GET['action'] ?? '') === 'token')) {
    ok('', ['csrf_token' => $csrf]);
}

if ($method !== 'POST') {
    fail(405, 'Método no permitido.');
}

// --- Rate limit: per IP, stored in session ---
$ip = $_SERVER['REMOTE_ADDR'] ?? '0.0.0.0';
$rlKey = 'rl_' . hash('sha256', $ip);
$now = time();

if (!isset($_SESSION[$rlKey]) || !is_array($_SESSION[$rlKey])) {
    $_SESSION[$rlKey] = ['count' => 0, 'reset' => $now + RATE_WINDOW];
}
if ($now >= $_SESSION[$rlKey]['reset']) {
    $_SESSION[$rlKey] = ['count' => 0, 'reset' => $now + RATE_WINDOW];
}
if ($_SESSION[$rlKey]['count'] >= MAX_RATE) {
    fail(429, 'Demasiados intentos. Intente más tarde.');
}
++$_SESSION[$rlKey]['count'];

// --- Honeypot: bots fill the hidden "website" field ---
if (!empty($_POST['website'])) {
    ok('Mensaje enviado correctamente.');
}

// --- CSRF ---
$token = (string)($_POST['csrf'] ?? '');
if ($token === '' || !hash_equals($csrf, $token)) {
    fail(403, 'Sesión inválida. Recargue la página.');
}

// --- Raw input ---
$name  = trim((string)($_POST['nombre'] ?? ''));
$email = trim((string)($_POST['email'] ?? ''));
$phone = trim((string)($_POST['telefono'] ?? ''));
$text  = trim((string)($_POST['mensaje'] ?? ''));

// --- Reject header injection characters (CR/LF/NUL) on every field ---
foreach ([$name, $email, $phone, $text] as $field) {
    if (preg_match('/[\r\n\0]/', $field)) {
        fail(422, 'Datos inválidos.');
    }
}

// --- Validation ---
$invalid = [];
if (mb_strlen($name) < 2 || mb_strlen($name) > 60) {
    $invalid[] = 'nombre';
}
if (mb_strlen($email) > 120 || !filter_var($email, FILTER_VALIDATE_EMAIL)) {
    $invalid[] = 'email';
}
if (mb_strlen($phone) > 20 || !preg_match('/^[0-9+\-\s().]{6,20}$/', $phone)) {
    $invalid[] = 'telefono';
}
if (mb_strlen($text) < 10 || mb_strlen($text) > 2000) {
    $invalid[] = 'mensaje';
}

if ($invalid) {
    fail(422, 'Datos inválidos en: ' . implode(', ', $invalid));
}

// --- Compose and send ---
$subject = 'Nuevo mensaje desde zelenisvet.com.ar';
$body = "Nombre: {$name}\n"
      . "Email: {$email}\n"
      . "Teléfono: {$phone}\n"
      . "-----------------------------\n"
      . "{$text}\n";

$headers = "From: Web ZELENI SVET <no-reply@zelenisvet.com.ar>\r\n"
         . "Reply-To: {$email}\r\n"
         . "Content-Type: text/plain; charset=UTF-8\r\n"
         . "MIME-Version: 1.0\r\n"
         . "X-Mailer: PHP/" . phpversion();

$encodedSubject = '=?UTF-8?B?' . base64_encode($subject) . '?=';

$sent = @mail(DESTINATION, $encodedSubject, $body, $headers);

if ($sent) {
    $_SESSION[$rlKey]['count'] = 0;
    ok('Mensaje enviado correctamente.');
}

fail(500, 'Hubo un error al enviar el mensaje. Intente nuevamente.');
```

- [ ] **Step 2: Lint the file**

Run: `php -l enviar.php`
Expected: `No syntax errors detected in enviar.php`

- [ ] **Step 3: Integration test with the PHP built-in server**

Run:
```
php -S 127.0.0.1:8080 -t D:\Diagrama
```
Then in a second terminal:
1. `curl -s http://127.0.0.1:8080/enviar.php?action=token` → JSON containing `"csrf_token":"..."` and `"ok":true`
2. `curl -s -X POST http://127.0.0.1:8080/enviar.php -d "csrf=INVALID&nombre=Test&email=a@b.com&telefono=3513001702&mensaje=Buenas, quiero una cotización."` → `{"ok":false,"message":"Sesión inválida. Recargue la página."}` with HTTP 403
3. `curl -s -i -X POST http://127.0.0.1:8080/enviar.php -d "website=spambot"` → `{"ok":true,"message":"Mensaje enviado correctamente."}` (honeypot)
4. With a valid CSRF token (from step 1), POST invalid email → `{"ok":false,"message":"Datos inválidos en: email"}` HTTP 422
5. With a valid CSRF token, POST valid data → local `mail()` will likely fail → HTTP 500 "Hubo un error..." (expected locally; real mail only works on Hostinger). If the machine has a mailer, it may return 200.

Stop the PHP server after the test.

- [ ] **Step 4: Commit**

```bash
git add enviar.php
git commit -m "feat: hardened contact form with CSRF, rate limit, honeypot, validation"
```

---

## Task 7: Server hardening (`.htaccess`, `robots.txt`, `404.html`)

**Files:**
- Create: `.htaccess`
- Create: `robots.txt`
- Create: `404.html`

- [ ] **Step 1: Write `.htaccess`**

Create `.htaccess`:

```apache
# --- Redirect HTTP to HTTPS ---
RewriteEngine On
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}/$1 [R=301,L]

# --- Disable directory listing ---
Options -Indexes

# --- UTF-8 as default charset ---
AddDefaultCharset UTF-8

# --- Deny access to hidden/dot files ---
<FilesMatch "^\.">
  Require all denied
</FilesMatch>

# --- Block sensitive files specifically ---
<FilesMatch "\.(lock|log|sql|bak|old|env|ini)$">
  Require all denied
</FilesMatch>

# --- Security headers ---
<IfModule mod_headers.c>
  Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
  Header always set X-Content-Type-Options "nosniff"
  Header always set X-Frame-Options "SAMEORIGIN"
  Header always set Referrer-Policy "strict-origin-when-cross-origin"
  Header always set Permissions-Policy "camera=(), microphone=(), geolocation=(), browsing-topics=()"
  Header always set Content-Security-Policy "default-src 'self'; img-src 'self' data:; font-src 'self'; style-src 'self'; script-src 'self'; form-action 'self'; frame-ancestors 'none'; frame-src https://www.google.com; base-uri 'self'; upgrade-insecure-requests"
</IfModule>

# --- Error pages ---
ErrorDocument 404 /404.html
```

- [ ] **Step 2: Write `robots.txt`**

Create `robots.txt`:

```
User-agent: *
Allow: /
```

- [ ] **Step 3: Write `404.html`**

Create `404.html`:

```html
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Página no encontrada - ZELENI SVET Consultora</title>
  <link rel="stylesheet" href="css/styles.css">
</head>
<body>
  <main class="section">
    <div class="container" style="text-align:center;">
      <h1>404 - Página no encontrada</h1>
      <p>El contenido que buscás no existe o fue movido.</p>
      <a class="btn" href="index.html">Volver al inicio</a>
    </div>
  </main>
</body>
</html>
```

> **Note:** The inline `style` on `404.html` violates the strict CSP, but the error page itself is not served with those headers enforced this way in all hosts; to stay fully compliant, replace it with a `.container--center { text-align:center; }` rule. Add to `css/styles.css`:
> ```css
> .container--center { text-align: center; }
> ```
> and use `class="container container--center"`.

- [ ] **Step 4: Commit**

```bash
git add .htaccess robots.txt 404.html css/styles.css
git commit -m "feat: server hardening (headers, CSP, protections)"
```

---

## Task 8: Final verification

**Files:** none (checks only)

- [ ] **Step 1: PHP lint**

Run: `php -l enviar.php`
Expected: `No syntax errors detected in enviar.php`

- [ ] **Step 2: Confirm no third-party references in the bundle**

Run: `rg -n "https?://" index.html js/main.js css/ | rg -v "google.com/maps"` 
Expected: no output (only the Google Maps iframe is external, and it is explicitly allowed by `frame-src`).

- [ ] **Step 3: Confirm original content preserved**

Open `index.html` and compare against the original site copy:
- Hero H1 "Bienvenidos a ZELENI SVET Consultora" + subtitle text
- 12 service cards with identical descriptions
- CTA "¿Necesitas una solución específica? ..."
- Projects: title "Proyecto premiado por la Universidad Católica de Córdoba", 4 items + captions
- Contact: "Pongámonos en Contacto", email, phones, cert caption, form labels
- Footer: "© 2025 ZELENI SVET Consultora. Todos los derechos reservados." + "Diseño Web Sostenible."

- [ ] **Step 4: Responsive + form behavior check**

1. Serve via `php -S 127.0.0.1:8080 -t D:\Diagrama` and open `http://127.0.0.1:8080/`:
   - Header sticky, menu collapses on mobile (< 768px), hamburger works
   - Submit button disabled until CSRF token loads, then enabled
   - Invalid data shows inline error from server (422); honeypot silently "succeeds"
2. Open `index.html` directly (file://): page renders; form shows the friendly connection error fallback (no PHP).
3. Check contrast of `#212121` on white (AA) and white on `#1B5E20` (AA).

- [ ] **Step 5: Deploy checklist**

For Hostinger upload, confirm before going live:
- Upload entire `D:\Diagrama` contents (index.html, enviar.php, .htaccess, css/, js/, assets/) to `public_html`
- The `.htaccess` rules apply (enabled on Hostinger Apache)
- Test live: `curl -sI https://zelenisvet.com.ar/` shows `Strict-Transport-Security: max-age=31536000; includeSubDomains`, `Content-Security-Policy: default-src 'self' ...`, `X-Content-Type-Options: nosniff`
- Send one real message via the form on the live site to confirm `mail()` delivers to `informacion@zelenisvet.com.ar`

---

## Self-Review Notes

- Spec coverage: palette (Task 3 tokens), components (Task 4), form security (Task 6 + headers Task 7), fonts self-hosted (Task 1), favicon (Task 2), map + mailto + WhatsApp (Task 4), 404/robots (Task 7), verification (Task 8). All spec sections map to tasks.
- No placeholders: every file's content is fully inlined in its task.
- Type/name consistency: `#contactForm`, `#respuesta`, `#enviar-btn`, hidden input `name="csrf"` and honeypot `name="website"` are consistent across Task 4 HTML, Task 5 JS (`form.elements['csrf']`) and Task 6 PHP (`$_POST['csrf']`, `$_POST['website']`).